{
   "package": {
        "name": "stable", 
        "repo": "rexray",
        "subject": "emccode"
    },

    "version": {
        "name": "${SEMVER}",
        "desc": "${DSCRIP}",
        "released": "${RELDTE}",
        "gpgSign": false
    },

    "files": [
        {
            "includePattern": ".bin/(.+)/(.*\.tar\.gz)",
            "excludePattern": ".bin/latest/(.*)",
            "uploadPattern": "stable/${SEMVER}/$2"
        },
        {
            "includePattern": ".bin/latest/(.*)",
            "uploadPattern": "stable/latest/$1"
        }
    ],

    "publish": true
}
